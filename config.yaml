# Global config for experiments
data:
  raw_path: "data/raw/data.csv"
  processed_path: "data/processed/data_processed.csv"

training:
  batch_size: 32
  learning_rate: 0.001
  epochs: 50
  seed: 42

model:
  type: "linear"

output:
  model_dir: "models/"
  logs_dir: "logs/"
  output_dir: "outputs/"
